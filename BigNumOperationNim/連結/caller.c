#include "fib.h"
#include <stdio.h>
#include <stdlib.h>

int main(void){
    NimMain();
    printf("%d", fib(10));
    
    HugeNum* a = malloc(sizeof(HugeNum));
    tySequence__U48dCdy0noze9bNL2psZZGA* g = get_seq2();
    free(g);
    
    return 0;
}